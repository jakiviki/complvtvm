<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.complvtvm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeCarmeloActivity"/>
        <activity
            android:name=".ListaDojoActivity"
            android:parentActivityName=".DojoActivity" />
        <activity
            android:name=".ListaBoxActivity"
            android:parentActivityName=".BoxActivity" />
        <activity
            android:name=".DojoActivity"
            android:parentActivityName=".ListGuerreroActivity" />
        <activity
            android:name=".BoxActivity"
            android:parentActivityName=".ListGuerreroActivity" />
        <activity android:name=".ListGuerreroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>